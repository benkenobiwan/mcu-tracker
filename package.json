{
  "name": "mcu-tracker",
  "private": true,
  "scripts": {
    "build": "node scripts/generate-config.js",
    "sync-navbar": "node scripts/sync-navbar-component.js",
    "dev": "npm run build && npx serve MCU-Tracker"
  }
}
